# 《我蛇了》剧情制作约定

## 当前唯一运行数据

游戏实际读取 `prototype/story-data.js`。Arcweave 原稿保存在 `story/source/project_settings.json`，规范化中间数据和待确认报告位于 `story/generated/`；它们不会未经确认直接覆盖运行数据。

第一章供制作人集中修改的文字母稿为 `story/第一章对白总稿.md`。该文件统一收录已实装对白、旁白、选项和后续草案；制作人可以整份修改后交回。程序实装完成并通过测试后，再同步 `[已实装]` 标记。运行时仍以 `prototype/story-data.js` 为准，避免未经确认的草案自动进入游戏。

拿到新的 Arcweave 原始导出后，应保留原始文件，并运行 `node tools/import-arcweave.js story/source/project_settings.json` 和 `node tools/arcweave-tests.js`。转换器保留 UUID、连接和段落边界；不要再手工维护多份对白。

## 当前游玩与存档规则

- 剧情模式当前固定为单人；全局的双人设置会保留，返回沙盒或 Boss 挑战后仍然生效。
- 剧情提供 3 个独立存档位，在进入每张地图时自动保存检查点。
- 剧情模式暂停菜单可以加载当前槽位最近一次自动存档，允许玩家适度回档。
- “继续游戏”从最近地图入口重建确定性场景，不尝试恢复半截战斗的瞬时对象。
- 旧的单槽 `imsnake.story.main` 会作为存档 1 读取，继续后写入新版三槽格式。
- 损坏存档不会阻止进入菜单，可以在对应槽位明确选择覆盖新开。

## 对话分页规则

- 一个或多个空行表示一次翻页。
- 普通换行仍属于同一页。
- 点击对白正文、“继续”按钮，或用键盘确认，每次只前进一页。
- 只有最后一页显示选项。
- `pages` 数组可以显式指定页面，优先级高于自动按空行拆分。
- 对话确认使用独立的“确认/交互”动作，默认回车；吐豆键不能推进对白。
- 面板刚出现和每次翻页后都必须等确认键松开，防止长按或连按跳过下一页。
- 教学提示使用 `{fire}`、`{interact}`、`{move}`、`{node}` 等动态占位，显示玩家当前自定义键位，不写死 J、F、空格或回车。
- 对话使用右侧纵向面板，只显示当前句；发言者变化时同步切换名字和头像。
- 进入人物或物品对话时，镜头平滑聚焦蛇头与互动目标；结束后恢复正常视野。

## 第一章当前可玩范围（v0.41）

- 序章输入名字后直接进入森林；森林持续刷新普通豆，并在小花和铁剑前放置固定豆与两只史莱姆。
- 吃普通豆累计 10 次或吐普通豆累计 10 次，任一条件满足后只触发一次饥饿对白；特殊物品不参与计数。
- 铁剑可以无视小花直接互动、堆叠吞入胃袋、逐把吐出和再次回收；吐出的铁剑碰到阿杰或丽丝也会立即触发认剑。NPC 识别后不会收走铁剑。
- 胃袋默认承重 6：人物重 3、铁剑重 2、骨头重 1、豆子重 0；物品越重飞行阻力越大。超重时蛇无法移动，身体闪红且头顶显示“超重！”，仍可吐出物品卸重。
- 被吐出的人物会飞行并保留角色身份与生命值；撞墙时自身受到 1 颗豆子的伤害，撞敌时双方受到等额伤害，落地后保持昏迷。
- 阿杰按 2 格/秒巡游，丽丝按 1 格/秒巡游；中立时免疫投射物和监狱伤害，可互动角色头顶显示对话气泡。
- 首次相遇、吞下任一角色、吐回昏迷角色、吐剑认领、接取或拒绝“寻找阿见”以及阿杰基础追击/击倒路径已经接入。
- 胃袋只显示当前持有物品；左上角任务栏可以同时列出多个进行中目标，再次碰到二人不会重复初见和接任务对白。
- 吞下可蒂后，她会进入胃袋；史莱姆战结束 6 秒后触发反胃演出，豆子与可蒂依次吐出，蛇身逐节缩短。
- 森林与河岸地图已扩展为 100×60，并标出山地、河流、河边营地、洞口、悬崖、吊桥和桥桩；山贼事件暂不生成。
- 哥布林洞窟为 64×32 横向长条，严格按地图稿布置左侧入口、中央房间、右侧祭坛和左端出口。桥桩需要用环形节点围住并累计充能 6 秒，完成后吊桥落下。
- 洞窟营救流程已可玩：热汤一次性治疗、回复药水拾取与消耗、舔脸或舔脚唤醒阿见、咬断绳索、哥布林弓箭手战斗、误伤濒死、药水救治、吞吐后重新唤醒、铁剑识别和骑乘。
- 战斗中离开洞窟会保存哥布林位置与生命、阿见生命和绳索状态；返回后从战斗继续。圆环教学、热汤和药水箱均不会重复发放。
- 河边营地人物交接、章末入口、荒野遗弃死亡和章末消化结算仍等待后续内容接入。

## 第一章洞窟已确认并实装

- 丽丝是阿杰和阿见的同行伙伴；阿杰与阿见是兄弟。
- 剧情模式此前禁用环形节点；哥布林洞窟祭坛是首次遇到黄色圆环，吃下后解锁能力并弹出一次环形节点文字教学。
- 两名哥布林弓箭手从祭坛方向走向阿见，抵达接近点后进入战斗；当前地图数据已记录出发点和接近点，等阿见苏醒节点接入时触发。
- 阿见被吐出后昏迷，可以再次吞下，或通过舔脸、舔脚叫醒；濒死状态只能短暂醒来，仍需治疗。相关状态会随存档恢复。
- 回复药水命中人物、玩家自己或墙壁后都会碎裂，不能回收。
- 阿见可以进入胃袋，也可以骑在蛇头后第一节身体上返回营地；骑乘者显示在头后第一节、重量计入负重，并随地图和存档恢复。
- 金币成为正式剧情状态；食欲值转入 GDD 待办，重新设计完成前不接入游戏。
- 阿杰与丽丝在铁剑互动和森林首次会面都完成后返回河边营地；任意一人清醒即可带上同一区域内昏迷的同伴，抵达后两人都恢复清醒。单人营地分支只用于另一人仍在胃袋、已经死亡或被遗留在其他区域。

示例：

```text
可蒂：是你救了我吗？

可我……不能离开这里。

请继续往前走吧，大人。
```

以上内容应显示三次。

## 设计一个剧情节点时需要提供什么

建议以后每个节点按下面的模板描述。不会发生的项目可以写“无”，不需要写程序术语。

```text
节点名称：
场景/地图：
进入节点时发生什么：

对白：
第一句

第二句

第三句

玩家选项：
- 选项文字 → 去向

正常推进条件：
玩家可以自由做什么：
暂时禁止什么：

如果玩家提前完成目标：
如果关键 NPC 提前死亡：
如果玩家离开区域：
如果玩家自己死亡：
如果重复触发：
如果两名玩家同时触发：
如果倒计时和玩家操作同时发生：

完成后进入：
重试时从哪里恢复：
```

## 默认异常处理规则

没有特别说明时，使用以下默认值：

1. 同一交互只接受第一个有效触发，重复触发不重复发奖、不重复改状态。
2. 双人同时触发时共享同一剧情状态；先到达的有效事件生效，另一人的同帧重复事件忽略。
3. 玩家提前完成目标时记录结果，剧情走到该条件时立即继续，不要求重复完成。
4. 关键 NPC 死亡、被吃、离开必须有明确后备节点；不得继续等待已经不可能发生的对话。
5. 对话期间默认冻结会改变剧情状态的操作；如果某场对话允许战斗，需要单独注明。
6. 玩家死亡后恢复到最近的地图检查点，不把按钮写成含义不准确的“重试本段”。
7. 倒计时与玩家主动操作同帧发生时，优先采用玩家主动操作。
8. 每个分支都必须能到达终点、下一章或明确的自由探索状态。

## Arcweave 的职责边界

Arcweave 负责作者路径：对白、选择、主流程和主要条件。

游戏运行补充表负责现实操作：误杀、逃跑、提前完成、重复触发、死亡、超时及双人冲突。两者合起来才是可实现的完整剧情规格。
